#include "main.h"

/**
 * _abs - comutees the absolute value of an integer
 * @n: the integer to be checked
 * Return: Absolute integer value
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
